name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Tests + Análisis de Seguridad
    runs-on: ubuntu-latest

    steps:
      # 1. Descargar el código
      - name: Checkout código
        uses: actions/checkout@v4

      # 2. Configurar Python
      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # Cachea las dependencias para que el pipeline corra más rápido

      # 3. Instalar dependencias
      - name: Instalar dependencias
        run: |
          python -m pip upgrade pip
          pip install -r requirements.txt
          pip install bandit # Requerido para el análisis de seguridad

      # 4. Ejecutar tests de Django
      - name: Ejecutar tests
        run: |
          python manage.py test

      # 5. Análisis SAST con Bandit (Seguridad en Python)
      - name: 🔒 Análisis de seguridad (Bandit)
        run: |
          bandit -r . -x ./venv,./tests